[global]
# ARI (Asterisk REST Interface) configuration
# this using making call manually(Click2call service)
# If not using click2call service, forgot it
ari_link=http://localhost:8088/ari/channels?
ari_auth=bW9uYXN0OmFiYzEyMw==  # Base64 encoded credentials

# License key for system validation, if not autocall system, forgot it
license_key=DbGltaXRfZGF0ZT0xNzgwMjc0MTQ1D

# Redis server configuration
redis_host=localhost
redis_port=6379

# Network binding configuration
#if not using click2call service, forgot it
bind_host = 0.0.0.0  # Listen on all interfaces
bind_port = 5044     # Main service port
bind_host_click2call=0.0.0.0  # Click2call service host
bind_port_click2call=5045     # Click2call service port
bind_port_automation=5050     # Automation service port

# MySQL database connection settings
mysql_host=localhost
mysql_user=asterisk_user
mysql_pass=asterisk_pass
mysql_database=asterisk

# API authentication credentials, it is using when call API to get data from mysql to redis
request_api_user=duonghb
request_api_pass=abc123

# API endpoints for call logging and campaign management
#if not using autocall system, forgot it
call_api_link=https://internal-tcb-production-debt-int-alb-520098912.ap-southeast-1.elb.amazonaws.com/api/AutoDialer/CallLog
call_api_link_finish=https://internal-tcb-production-debt-int-alb-520098912.ap-southeast-1.elb.amazonaws.com/api/AutoDialer/EndCampaign
